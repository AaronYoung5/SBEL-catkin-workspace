#pragma once

namespace ProtobufCodes {
        enum Code {LIDAR, GPS, IMU, VEHICLE, CONTROL, TIME, LIGHT, CONE, EXIT};
}
namespace PublisherCodes {
        enum Code {LIDAR, GPS, IMU, VEHICLE, TIME, CONE};
}
